OBJS = uimain.o tokenizer.o


all:test

#Pass all objects listed at top to the file
test: $(OBJS)
	cc -o test $(OBJS)

#All obj files that are built from c files that include tokenizer.h
$(OBJS): tokenizer.h


#Deletes all the files generated by compilation
clean:
	rm -f *o test

run:test
	./test
